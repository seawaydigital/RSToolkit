# This release needs no API keys or application secrets.
# Optional build-time base path, with leading and trailing slash:
# BASE_PATH=/research-security/
# VITE_ variables are public in browser bundles. Never put secrets in them.
# The previous map provider variables have no effect in this release.
